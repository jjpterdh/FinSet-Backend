<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="security.account.mapper.UserDetailsMapper">
    <resultMap id="authMap" type="AuthVO">
        <result property="username" column="user_name"/>
        <result property="auth" column="auth"/>
    </resultMap>

    <resultMap id="memberMap" type="MemberVO">
        <id property="uno" column="uno"/>
        <result property="userId" column="user_id"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="createAt" column="createAt"/>


        <collection property="authList" resultMap="authMap"/>
    </resultMap>

    <select id="get" resultMap="memberMap">
        SELECT m.uno, m.user_id, m.password, m.email, m.createAt, m.user_name, a.auth
        FROM tbl_member m
                 LEFT OUTER JOIN tbl_member_auth a ON m.user_id = a.username
        WHERE m.username = #{userId}
    </select>
</mapper>
